%h1 Listing plans

%table
  %tr
    %th
    %th
    %th

  - @plans.each do |plan|
    %tr
      %td= link_to plan.description, plan
      %td= link_to 'Edit', edit_plan_path(plan)
      %td= link_to 'Destroy', plan, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Plan', new_plan_path

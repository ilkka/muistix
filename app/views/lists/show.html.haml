%h1= @list.description

%p#notice= notice

= link_to 'Edit', edit_list_path(@list)
\|
= link_to 'Delete', @list, :confirm => 'Are you sure?', :method => :delete
\|
= link_to 'Back', lists_path

%ol
  - @list.steps.each do |step|
    %li= link_to step.objective, step.uri
